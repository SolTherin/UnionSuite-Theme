const fs=require('node:fs'),path=require('node:path');
const root=path.resolve(__dirname,'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8');
const esc=s=>String(s).replaceAll('&','&amp;').replaceAll('"','&quot;').replaceAll('<','&lt;').replaceAll('>','&gt;');
const theme=read('THeme/UnionSuite/zUnionSuite.css'),iconSource=read('THeme/UnionSuite/Tabler/tabler-icons.min.css');
const font=fs.readFileSync(path.join(root,'THeme/UnionSuite/Tabler/fonts/tabler-icons.woff2')).toString('base64');
const icons='@font-face{font-family:tabler-icons;src:url(data:font/woff2;base64,'+font+') format("woff2")}'+['pencil','settings','plus','trash','x'].map(name=>{const rule=iconSource.match(new RegExp('\\.ti-'+name+':before\\{[^}]+\\}'));if(!rule)throw Error(name);return rule[0];}).join('');

const nativeAdapter=read('THeme/UnionSuite/zUnionSuite.js').match(/\/\* US-DATA-PANELS:START[\s\S]*?US-DATA-PANELS:END \*\//)[0];
const nativeFoundation=(read('Native CSS/10-UltraWaveResponsive.css')+'\n'+read('THeme/UnionSuite/99-Orion.css')).replace(/@import\s+[^;]+;/g,'').replace(/@font-face\s*\{[^}]*\}/g,'');
const nativeFrames=['read','organisation','edit','multi'].map(name=>{const doc='<html><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><style>'+nativeFoundation+theme+icons+'html,body{height:auto}body{padding:16px;margin:0;background:#eeeef0}</style><body>'+read('prototypes/Data-Panel-Native-'+name+'.html')+'<script>'+nativeAdapter+';document.addEventListener("click",e=>{if(e.target.closest("a,input[type=image],button,input[type=submit]"))e.preventDefault()},true);new ResizeObserver(()=>parent.postMessage({type:"data-panel-size",height:document.body.scrollHeight+16},"*")).observe(document.body);<\/script></body></html>';return '<iframe id="native-live-'+name+'" title="Native '+name+' panel with shared theme" srcdoc="'+esc(doc)+'"></iframe>';}).join('');

function frame(proposed){const id=proposed?'proposed':'native';
  const action=(kind,label,icon)=>`<button type="button" data-${kind} class="${proposed?'us-iqa-icon-button':'native-action'}" aria-label="${label}" title="${label}"><i class="ti ti-${icon}" aria-hidden="true"></i></button>`;
  const html=`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><style>${theme}\n${icons}\n${read('prototypes/Data-Panel-Comparison.css')}</style><body data-variant="${id}"><div class="ContentItemContainer ${proposed?'us-data-panel-trial':''}" ${proposed?'data-us-panel="single"':''}><section class="panel"><header class="panel-heading Distinguish"><h2 class="panel-title">Contact details</h2><div class="panel-heading-options ${proposed?'us-iqa-report-utilities':''}">${action('settings','Panel settings','settings')}${action('edit','Edit contact details','pencil')}</div></header><div class="panel-body-container"><div class="panel-body"><form><div class="field-grid"></div><div class="LocalButtonBar" hidden><button class="PrimaryButton TextButton" type="submit">Save</button><button class="TextButton" type="button" data-cancel>Cancel</button></div></form></div></div></section></div><p class="preview-status" role="status"></p><dialog aria-labelledby="settings-title"><h2 id="settings-title">Panel settings</h2><p>This button represents the existing iMIS panel configuration action. Its native designer is outside this visual comparison.</p><p>The new icon retains the original control and handler.</p><button class="TextButton" type="button" data-close>Close</button></dialog><script>${read('prototypes/Data-Panel-Fixture.js')}</script></body></html>`;
  return `<iframe id="${id}" title="${proposed?'Proposed IQA-style data panel':'Native data panel reconstruction'}" srcdoc="${esc(html)}"></iframe>`;
}
const multi=proposed=>require('./data-panel-multi-example.cjs')({proposed,theme,icons,read,esc});
const html=`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Data panels — native / proposed</title><style>
*{box-sizing:border-box}body{margin:0;background:#f5f6f7;color:#26343b;font:15px/1.6 system-ui}main{max-width:1560px;margin:auto;padding:32px}a{color:#006f94}h1{font-size:30px;color:#003a4c;margin:20px 0 8px}h2{font-size:19px;margin:0 0 6px}.badge{font-size:11px;background:#ffead8;color:#72431a;padding:5px 9px;border-radius:20px;vertical-align:middle}.intro{max-width:850px;color:#58646b;margin:8px 0}.toolbar{display:flex;flex-wrap:wrap;align-items:center;gap:16px;padding:16px;background:white;border:1px solid #e0e4e6;border-radius:10px;margin:24px 0}label{display:flex;gap:7px;align-items:center}select,button{font:inherit;padding:7px 10px;border:1px solid #ccd2d6;border-radius:5px;background:white;color:#26343b}button{cursor:pointer}.compare{display:grid;grid-template-columns:1fr 1fr;gap:24px}.note{min-height:52px;color:#657079;font-size:13px}iframe{width:100%;height:570px;display:block;border:1px solid #dce1e4;border-radius:10px;background:#eeeef0}body[data-width=narrow] .compare{grid-template-columns:1fr 1fr}body[data-width=narrow] iframe{max-width:390px;margin:auto}.reference{background:white;border:1px solid #e0e4e6;border-radius:10px;padding:24px;margin-top:32px}li{margin-bottom:8px}code{font-size:12px}pre{overflow:auto;padding:16px;background:#f5f6f7}summary{cursor:pointer} @media(max-width:1150px){.compare,body[data-width=narrow] .compare{grid-template-columns:1fr}.note{min-height:0}main{padding:20px}}@media(max-width:500px){main{padding:12px}h1{font-size:25px}.toolbar{gap:12px}}
</style><body data-width="full"><main><a href="index.html">← All component references</a><h1>Data display panels <span class="badge">APPROVED</span></h1><p class="intro">The IQA header and its compact utility buttons, carried into the native Panel Editor. A quieter white body keeps the record values distinct from their labels.</p><p class="intro">Compare read-only and edit modes, including empty fields and read-only metadata. Both sides use fictional data; Save changes this example only.</p><div class="toolbar"><label>Mode <select id="mode"><option value="read">Read only</option><option value="edit">Edit</option></select></label><label>Width <select id="width"><option value="full">Full width</option><option value="narrow">Mobile · 390px</option></select></label><label><input type="checkbox" id="filled"> Fill empty fields</label><button type="button" id="reset">Reset examples</button></div><h2 id="single-heading">Single-instance panels</h2><p>One set of configured values per member. Field order and grouping remain author-defined.</p><div class="compare"><section><h2>Native / current structure</h2><p class="note">Reconstructed from your supplied HTML and screenshots: open heading, muted icons and dotted field dividers.</p>${frame(false)}</section><section><h2>Approved / IQA family</h2><p class="note">Shared IQA header and action-button CSS. White surface, subtle dividers, clear empty values and a separated Save/Cancel footer.</p>${frame(true)}</section></div><h2 id="multi-heading" style="margin-top:32px">Multi-instance panels</h2><p>Multiple rows per member. The same header treatment, with Add in the header and Edit/Delete on each row. Use the width control above to compare mobile layouts.</p><div class="compare"><section><h2>Native / current structure</h2><p class="note">Open heading, settings/add icons and row edit/delete controls, reconstructed from your example.</p>${multi(false)}</section><section><h2>Approved / IQA family</h2><p class="note">IQA header, sortable grid headings with active-column highlights, and theme pencil/trash icons. Delete uses the theme’s red hover and focus treatment.</p>${multi(true)}</section></div><section class="reference"><h2>Native markup integration</h2><p>Captured native single-instance, editable upload and multi-instance structures with the shared panel adapter. Commands are inert here; use iMIS for live testing.</p>${nativeFrames}</section><section class="reference"><h2>Approved treatment</h2><ul><li><strong>Header:</strong> the actual IQA surface, title typography, padding and rounded corners.</li><li><strong>Actions:</strong> settings and edit retain their separate meanings, using Tabler glyphs inside the existing IQA utility-button treatment. Hover and keyboard focus are visible; mobile targets are 44px.</li><li><strong>Body:</strong> three columns on desktop, two on smaller panels and one at 400px or below. The same field order is used in both modes; these example columns are not a required layout for configurable panels. Empty read-only values use a quiet dash.</li><li><strong>Multi-instance:</strong> Add and Edit open an example modal; deleting a demo row asks for confirmation. Rows stay in their configured table order, including at mobile widths via horizontal scrolling.</li><li><strong>Uploads:</strong> Resume demonstrates the shared Theme Upload / Content Import treatment for native RadUploadPanel controls. Choose a file locally; this example never uploads it. File rules shown here belong to this sample, not a theme default.</li><li><strong>Editing:</strong> native-style controls, retained audit metadata, Save and Cancel. Edits are held only in memory; Cancel discards them.</li></ul><p><strong>Implemented:</strong> shared CSS and UnionSuiteDataPanels detect native panels automatically. No us-report class is required. The examples below use the original native structure with server commands disabled.</p><p>Single-instance mode and empty-field controls affect only the first comparison; Reset and Width apply to both types. The integration targets <code>.PanelEditorReadOnlyForm</code> and <code>.PanelEditorEditForm</code>, preserve generated IDs, existing Telerik buttons, validation and AJAX replacement, and separate panel configuration from record editing. The date field here uses a browser-native date input; live iMIS retains its Telerik calendar.</p><details><summary>Source and implementation boundary</summary><p>Generator: <code>tools/build-data-panel-comparison.cjs</code>. Trial body CSS: <code>prototypes/Data-Panel-Comparison.css</code>. Offline interactions: <code>prototypes/Data-Panel-Fixture.js</code>. Header/action CSS and colour tokens are embedded directly from <code>zUnionSuite.css</code> at build time; Tabler font data is embedded for offline use.</p></details><p><a href="../THeme/UnionSuite/Usage-Guide.html#data-panels-trial">Usage guide entry</a></p></section></main><script>
const frames=[...document.querySelectorAll('iframe')];const send=(action,value)=>frames.forEach(f=>f.contentWindow.postMessage({type:'data-panel-command',action,value},'*'));
document.querySelector('#mode').onchange=e=>send('mode',e.target.value);document.querySelector('#filled').onchange=e=>send('filled',e.target.checked);document.querySelector('#width').onchange=e=>document.body.dataset.width=e.target.value;document.querySelector('#reset').onclick=()=>{document.querySelector('#mode').value='read';document.querySelector('#filled').checked=false;send('reset');};
window.addEventListener('message',e=>{const frame=frames.find(f=>f.contentWindow===e.source);if(frame&&e.data?.type==='data-panel-size'&&Number.isFinite(e.data.height))frame.style.height=Math.max(220,Math.min(2400,e.data.height))+'px';});
</script></body></html>`;
const output=path.join(root,'references/Data-Panel-Comparison.html');
if(process.argv.includes('--check')){if(!fs.existsSync(output)||fs.readFileSync(output,'utf8')!==html){console.error('Data panel comparison is stale.');process.exitCode=1;}else console.log('Data panel comparison is current.');}else{fs.writeFileSync(output,html);console.log('Built references/Data-Panel-Comparison.html');}
