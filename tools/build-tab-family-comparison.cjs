const fs=require('fs');
const names=['Overview','About','Finance','Notes'];
function buttons(id,cls){return names.map((n,i)=>`<button type="button" role="tab" id="${id}-tab-${i}" aria-controls="${id}-panel" aria-selected="${i===0}" tabindex="${i===0?0:-1}" class="${cls}${i===0?' is-active':''}">${n}</button>`).join('');}
function native(id,vertical){return `<div class="${vertical?'RadTabStripVertical':'RadTabStrip'}"><div class="rtsLevel rtsLevel1"><ul class="rtsUL" role="tablist" aria-label="${vertical?'Vertical':'Horizontal'} CCO sections" ${vertical?'aria-orientation="vertical"':''}>${names.map((n,i)=>`<li class="rtsLI" role="presentation"><a href="#${id}-panel" role="tab" id="${id}-tab-${i}" aria-controls="${id}-panel" aria-selected="${i===0}" tabindex="${i===0?0:-1}" class="rtsLink${i===0?' rtsSelected':''}"><span class="rtsOut"><span class="rtsIn"><span class="rtsTxt">${n}</span></span></span></a></li>`).join('')}</ul></div></div>`;}
function panel(id){return `<div class="RadMultiPage"><div class="sample-panel" id="${id}-panel" role="tabpanel" aria-labelledby="${id}-tab-0" tabindex="0"><small>MEMBER SECTION</small><h3>Overview</h3><p>Member details and summary information.</p><div class="sample-facts"><span>Member number<strong>103885</strong></span><span>Member type<strong>Regular member</strong></span></div></div></div>`;}
const cards=[
 ['banner','Current member banner','Shared production styling','Selected fill and status-coloured underline on navy. Actions keep their glass finish.',`<div class="ContentItemContainer"><div class="us-banner"><header class="us-banner__surface us-banner__surface--member" style="--member-status-colour:#23845B"><div class="us-banner__summary"><div class="us-banner__identity"><span class="us-banner__eyebrow">Member · 103885</span><h2 class="us-banner__title">Tony Fin Stark</h2></div><span class="us-banner__badge us-banner__badge--member-status">Financial member</span></div><div class="us-banner__nav"><div class="us-banner__tabs" role="tablist" aria-label="Banner sections">${buttons('banner','us-banner__tab')}</div></div></header></div></div>${panel('banner')}`],
 ['cco-h','Current horizontal CCO','Shared production styling','Attached folder tabs with the theme accent along the selected tab’s top edge.',`<div class="tabs-wrapper">${native('cco-h',false)}${panel('cco-h')}</div>`],
 ['cco-v','Current vertical CCO','Shared production styling','Sidebar navigation with an accent edge and tinted selected item.',`<div class="tabs-wrapper tabs-vertical">${native('cco-v',true)}${panel('cco-v')}</div>`],
 ['underline','Standalone · underline','Option A — current prototype','A quiet detached navigation row. Uses the theme accent instead of member status colour.',`<div class="trial-nav underline" role="tablist" aria-label="Underline sections">${buttons('underline','trial-tab')}</div>${panel('underline')}`],
 ['folder','Standalone · CCO family','Option B — reuse horizontal CCO appearance','The same folder-tab appearance, detached from the content card. This would share visual rules with CCO.',`<div class="detached">${native('folder',false)}</div>${panel('folder')}`],
 ['segment','Standalone · compact selection','Option C — contained selection','A softly inset track with a raised selected item and a small theme-accent indicator.',`<div class="trial-nav segment" role="tablist" aria-label="Compact sections">${buttons('segment','trial-tab')}</div>${panel('segment')}`]
];

const variations=[
 ['soft','Soft inset','Closest to #6: a softly rounded track and a raised white selection. The banner uses the same rounded rectangle.'],
 ['pill','Rounded capsule','A more rounded shape at both levels, with a small accent marker. Friendly, but more prominent.'],
 ['line','Crisp segmented','Tighter corners and an outlined selection, with less shadow. A more restrained option for dense screens.']
];
function subTabs(id){
 const labels=['Contact details','Addresses','Employment'];
 return '<div class="sub-nav" role="tablist" aria-label="Profile subsections">'+labels.map((n,i)=>'<button class="sub-tab'+(i===0?' is-active':'')+'" type="button" role="tab" id="'+id+'-tab-'+i+'" aria-controls="'+id+'-panel" aria-selected="'+(i===0)+'" tabindex="'+(i===0?0:-1)+'">'+n+'</button>').join('')+'</div><div class="sub-content" role="tabpanel" id="'+id+'-panel" aria-labelledby="'+id+'-tab-0" tabindex="0"><h3>Contact details</h3><p>Preferred contact information for this member.</p><div class="sample-facts"><span>Email<strong>tony@example.org</strong></span><span>Mobile<strong>0400 123 777</strong></span></div></div>';
}
function context(kind,id){
 const inner='<div class="context-body" id="'+id+'-panel" role="tabpanel" aria-labelledby="'+id+'-tab-0" tabindex="0"><h3>Overview</h3><p class="context-note">Member sections above · subsections below</p>'+subTabs(id+'-sub')+'</div>';
 if(kind==='banner')return '<div class="context banner-context"><p class="context-label">Under banner tabs · proposed matching shape</p><div class="ContentItemContainer"><div class="us-banner"><header class="us-banner__surface us-banner__surface--member" style="--member-status-colour:#23845B"><div class="us-banner__summary"><div class="us-banner__identity"><span class="us-banner__eyebrow">Member · 103885</span><h2 class="us-banner__title">Tony Fin Stark</h2></div><span class="us-banner__badge us-banner__badge--member-status">Financial member</span></div><div class="us-banner__nav"><div class="us-banner__tabs" role="tablist" aria-label="Member sections">'+buttons(id,'us-banner__tab')+'</div></div></header></div></div>'+inner+'</div>';
 return '<div class="context"><p class="context-label">Under '+(kind==='vertical'?'vertical':'horizontal')+' CCO · current parent styling</p><div class="tabs-wrapper '+(kind==='vertical'?'tabs-vertical':'')+'">'+native(id,kind==='vertical')+'<div class="RadMultiPage">'+inner+'</div></div></div>';
}
variations.push(
 ['accent-line','Accent underline','Option #6 shape, with a longer accent underline. White selection stays quiet; the colour carries the emphasis.'],
 ['accent-tint','Accent tint','Option #6 shape, with a pale accent fill and a fine accent outline. Stronger selection without a solid block of colour.'],
 ['accent-solid','Solid accent','Option #6 shape, filled with the theme accent. The strongest selection signal at both navigation levels.']
);
const selectedSix='<section class="exploration selected-six" id="selected-six"><div class="caption"><small>Selected direction — option 6</small><h2>Option 6 · compact selection</h2><p>Original segmented shape with inset shading while pressed. Matching banner tabs use neutral selection without a coloured accent.</p></div><div class="contexts">'+context('banner','six-banner')+context('horizontal','six-h')+context('vertical','six-v')+'</div></section>';
const fullBand='<section class="exploration selected-six full-band" id="six-full-band"><div class="caption"><small>Option 6B — banner variation</small><h2>Full-width darker tab area</h2><p>The same compact tabs, with the darker surface filling the entire area below the separator.</p></div>'+context('banner','six-band-banner')+'</section>';
const explorations=variations.map(([key,title,note],i)=>'<section class="exploration '+key+'" id="option-'+(i+7)+'"><div class="caption"><small>Option '+(i+7)+' — submenu direction</small><h2>'+(i+7)+'. '+title+'</h2><p>'+note+'</p></div><div class="contexts">'+context('banner',key+'-banner')+context('horizontal',key+'-h')+context('vertical',key+'-v')+'</div></section>').join('');

fs.writeFileSync('references/Tab-Family-Comparison.html',`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Tab styling comparison · Union Suite</title><link rel="stylesheet" href="../THeme/UnionSuite/zUnionSuite.css"><link rel="stylesheet" href="../prototypes/tab-family-comparison.css"></head><body><main><header class="intro"><p class="eyebrow">UNION SUITE / NAVIGATION FAMILY</p><h1>One theme. Different tab contexts.</h1><p>Current banner and CCO styles, alongside three standalone directions. Select a tab to compare its active, hover and keyboard states.</p><p class="muted">The first three use shared theme CSS. All switching here is a local simulation, including CCO. Production behaviour is unchanged.</p><a href="Standalone-Tabs-Preview.html">Back to working standalone prototype</a></header><p class="jump"><a href="#selected-six"><strong>Selected: option 6 in context</strong></a> · <a href="#six-full-band">6B. Full-width tab area</a><br><strong>Archived accent exploration:</strong> <a href="#option-10">10. Underline</a> · <a href="#option-11">11. Tint</a> · <a href="#option-12">12. Solid</a><br><a href="#option-7">7. Soft inset</a> · <a href="#option-8">8. Rounded capsule</a> · <a href="#option-9">9. Crisp segmented</a></p>${selectedSix}${fullBand}<details><summary>Earlier comparisons and alternatives</summary><div class="comparison">${cards.map(([id,title,tag,note,markup],i)=>`<section class="comparison-card ${id==='banner'?'wide':''}" data-demo="${id}"><div class="caption"><small>${tag}</small><h2>${i+1}. ${title}</h2><p>${note}</p></div><div class="example">${markup}</div></section>`).join('')}</div>${explorations}</details><aside><h2>What we’re choosing</h2><p>Option A is the lightest standalone treatment. Option B provides the closest match to existing horizontal CCOs. Option C feels more like a compact view selector. Banner navigation can keep its navy adaptation whichever standalone option we choose.</p><p>These are visual proposals. Shared appearance does not require shared loading behaviour: native CCO retains iMIS navigation; standalone sections can switch locally.</p></aside></main><script src="../prototypes/tab-family-comparison.js"></script></body></html>`);
