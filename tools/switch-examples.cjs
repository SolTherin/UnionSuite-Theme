const g=require('./theme-gallery.cjs');
function control(primary=false,icons=false){return `<label class="us-switch${primary?' us-switch--primary':''}">
  <input type="checkbox" role="switch">
  <span class="us-switch__track" aria-hidden="true">${icons?'<span class="us-switch__symbol"><i class="ti ti-x us-switch__symbol-off"></i><i class="ti ti-check us-switch__symbol-on"></i></span>':''}</span>
  <span>Email notifications</span>
  <span class="us-switch__state" aria-hidden="true"></span>
</label>`;}
const markup=`<section id="spring-switches"><h2>Spring toggle switches</h2><p>Implemented in the shared theme. Accent is the default; the main theme colour is the alternative. Click a label or use Tab then Space.</p><div class="cards">${[false,true].map((primary,i)=>`<article><h3>${primary?'Main theme colour':'Accent — default'}</h3><div class="sample">${control(primary)}${control(primary,true)}</div>${g.chip(primary?'us-switch us-switch--primary':'us-switch','On the label around the checkbox')}<p>Put the class on the authored label, inside Content HTML. Do not put it in the iPart CSS class field.</p>${g.recipe('spring-switch-'+i,control(primary,true),'Copyable switch HTML')}</article>`).join('')}</div><p>Load zUnionSuite.css and zUnionSuite.js. The optional symbol span uses Tabler.css; omit it for a plain thumb. Keep a stable visible label. Add checked for initially on, disabled for unavailable, and name/value when submitting a form. The optional us-switch__state span displays On/Off. A checkbox changes local state only: wire its change event to your own save operation. No IQA fields are involved.</p><p>52 × 30px track, 22px thumb, at least 44px label target. The 240ms spring slide and squash run on changes; rapid changes restart the squash. Reduced motion disables movement. Native checkbox keyboard, checked and disabled semantics are retained; replacing markup needs no refresh call. Accent maps to --accent, the alternative to --brand-600, with --bg-subtle and --text-muted for off and --brand-600 for focus.</p></section>`;
module.exports={markup};
