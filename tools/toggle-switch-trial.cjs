// Reference-only design trial. No production selectors or behaviour.
const css = `
.toggle-trial{margin:28px 0;scroll-margin-top:24px}
.toggle-trial .trial-label{color:var(--text-strong);font-weight:600}
.toggle-trial .trial-options{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px}
.toggle-trial .trial-switch{display:flex;align-items:center;gap:12px;min-height:44px;cursor:pointer;width:fit-content;position:relative;padding:4px}
.toggle-trial .trial-switch input{position:absolute;opacity:0;width:52px;height:32px;margin:0;inset-inline-start:4px;top:10px}
.toggle-trial .trial-track{box-sizing:border-box;display:block;flex:none;width:52px;height:30px;border:1px solid var(--text-muted,#677580);border-radius:999px;background:var(--bg-subtle,#e8ecef);position:relative;transition:background 160ms,border-color 160ms;box-shadow:inset 0 1px 2px #00000012}
.toggle-trial .trial-track:after{content:'';position:absolute;width:22px;height:22px;inset-inline-start:3px;top:3px;border-radius:50%;background:#fff;box-shadow:0 1px 3px #00000045;transition:inset-inline-start 160ms}
.toggle-trial input:checked + .trial-track{background:var(--accent);border-color:var(--accent)}
.toggle-trial input:checked + .trial-track:after{inset-inline-start:25px}
.toggle-trial .trial-symbol{position:absolute;z-index:1;inset-inline-start:3px;top:3px;width:22px;height:22px;display:grid;place-items:center;color:var(--text-muted,#677580);transition:inset-inline-start 160ms;font-size:15px;line-height:1;pointer-events:none}
.toggle-trial .trial-symbol .ti{grid-area:1/1}
.toggle-trial .trial-symbol-on{visibility:hidden}
.toggle-trial input:checked + .trial-track .trial-symbol{inset-inline-start:25px;color:var(--brand-800)}
.toggle-trial input:checked + .trial-track .trial-symbol-off{visibility:hidden}
.toggle-trial input:checked + .trial-track .trial-symbol-on{visibility:visible}
.toggle-trial .trial-navy input:checked + .trial-track{background:var(--brand-600);border-color:var(--brand-600)}
.toggle-trial input:focus-visible + .trial-track{outline:3px solid var(--brand-600);outline-offset:3px}
.toggle-trial .trial-switch:hover input:not(:disabled) + .trial-track{box-shadow:0 0 0 3px var(--accent-100,#fff0e6),inset 0 1px 2px #00000012}
.toggle-trial input:active:not(:disabled) + .trial-track:after{box-shadow:0 0 0 2px #00000020}
.toggle-trial .trial-switch:has(input:disabled){opacity:.5;cursor:not-allowed}
.toggle-trial .trial-state{font-size:12px;color:var(--text-muted);min-width:22px}
.toggle-trial .trial-state:after{content:'Off'}
.toggle-trial input:checked ~ .trial-state:after{content:'On'}
@media(max-width:650px){.toggle-trial .trial-options{grid-template-columns:1fr}}
@media(prefers-reduced-motion:reduce){.toggle-trial .trial-track,.toggle-trial .trial-track:after,.toggle-trial .trial-symbol{transition:none}}
@media(forced-colors:active){.toggle-trial .trial-symbol{color:Canvas}.toggle-trial input:checked + .trial-track .trial-symbol{color:Highlight}}
@media(forced-colors:active){.toggle-trial .trial-track{background:Canvas;border-color:ButtonText}.toggle-trial .trial-track:after{background:ButtonText}.toggle-trial input:checked + .trial-track{background:Highlight;border-color:Highlight}.toggle-trial input:checked + .trial-track:after{background:HighlightText}.toggle-trial input:focus-visible + .trial-track{outline-color:Highlight}}
`;
function control(label, checked=false, disabled=false, navy=false, symbols=null){const icon=symbols?`<span class="trial-symbol"><i class="ti ti-${symbols[0]} trial-symbol-off"></i><i class="ti ti-${symbols[1]} trial-symbol-on"></i></span>`:'';return `<label class="trial-switch${navy?' trial-navy':''}"><input type="checkbox" role="switch"${checked?' checked':''}${disabled?' disabled':''}><span class="trial-track" aria-hidden="true">${icon}</span><span>${label}</span><span class="trial-state" aria-hidden="true"></span></label>`}
const markup = `<section class="toggle-trial" id="toggle-switches"><h2>Toggle switches</h2><p><a href="Toggle-Motion-Comparison.html">Try Subtle, Spring and Spring + pulse animations →</a></p><p class="trial-label">Design trial — reference only</p><p>A rounded track and white thumb, using the same orange accent, neutral surfaces and navy focus colour as our buttons. On stays filled during hover so the setting remains clear. Click a label or use Tab then Space to try it.</p><div class="trial-options"><article><h3>Accent — proposed default</h3><p>Matches primary buttons and the default checkbox colour.</p>${control('Email notifications',true)}${control('Weekly summary')}</article><article><h3>Navy — alternative</h3><p>Uses the primary brand colour for a quieter settings treatment.</p>${control('Email notifications',true,false,true)}${control('Weekly summary',false,false,true)}</article><article><h3>Unavailable states</h3>${control('Enabled setting',true,true)}${control('Disabled setting',false,true)}<p>Disabled switches retain their current position and cannot be changed.</p></article><article><h3>In a settings row</h3>${control('Include archived records')}<p>A stable label names the setting; thumb position and On/Off text communicate its state. Use switches for independent settings, with separate save behaviour when required.</p></article><article><h3>Optional symbols — cross / check</h3><p>A small symbol inside the thumb reinforces the setting state.</p>${control('Email notifications',false,false,false,['x','check'])}${control('Email notifications',true,false,false,['x','check'])}</article><article><h3>Optional symbols — unlocked / locked</h3><p>Use a pair whose meaning matches the setting label.</p>${control('Lock editing',false,false,true,['lock-open','lock'])}${control('Lock editing',true,false,true,['lock-open','lock'])}</article><article><h3>Optional symbols — sun / moon</h3><p>The symbol represents the current state: sun when off, moon when on.</p>${control('Dark appearance',false,false,false,['sun','moon'])}${control('Dark appearance',true,false,false,['sun','moon'])}</article><article><h3>Same symbol in both states</h3><p>A bell identifies the setting; position and On/Off text still show its state.</p>${control('Notifications',false,false,true,['bell','bell'])}${control('Notifications',true,false,true,['bell','bell'])}${control('Notifications unavailable',true,true,true,['bell','bell'])}</article></div><p>Track: 52 × 30px. Thumb: 22px. Label target: at least 44px high. Motion: 160ms, removed with reduced motion. Native checkbox semantics provide keyboard and checked/disabled states. These examples only change local state; they do not save data.</p><p>This trial is maintained in the reference generator. A shared author class, copyable production template and installation recipe will follow when this reference is promoted to the shared theme. The rounded base design is accepted; optional symbol treatments are shown here for review. Symbols use the bundled Tabler icon font, remain decorative and never replace the visible label. Omit the symbol span for a plain thumb; use matching icons for a fixed symbol or separate off/on icons for changing states.</p></section>`;
module.exports={css,markup,control};
