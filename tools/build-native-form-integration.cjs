const fs=require('fs');
const read=p=>fs.readFileSync(p,'utf8');
const esc=s=>s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/"/g,'&quot;');
const css=(read('THeme/UnionSuite/guides/usage/vendor/10-UltraWaveResponsive.css')+'\n'+read('THeme/UnionSuite/99-Orion.css')+'\n'+read('THeme/UnionSuite/zUnionSuite.css')).replace(/@import[^;]+;/g,'').replace(/url\([^)]*\)/g,url=>url.includes('data:image/svg+xml')?url:'none');
const nativeSource=read('THeme/UnionSuite/zUnionSuite.css');
const trial=nativeSource.slice(nativeSource.indexOf('*/',nativeSource.indexOf('/* US-NATIVE-FORMS:START'))+2,nativeSource.indexOf('/* US-NATIVE-FORMS:END */'));
const fallback=read('prototypes/Native-Form-Integration.css').split('\n').filter(l=>l.includes('.ruFileInput')||l.includes(':is(.ruFakeInput,.ruBrowse,.ruClear)')||l.includes('.ruFileWrap')||l.includes('.EmptyMasterContentPanel:has')||l.includes('upload-preview-status')).join('\n').replaceAll('[id$="_AppThemeEditControl_UploadPanel"]',':is([id$="_AppThemeEditControl_UploadPanel"],[id$="_ImporterControlPanel"] [id$="_FileUploadPanel"])');
const items=['Theme-Upload','Edit-Address','Add-Contact','Panel-Editor','Import'];
const frame=(name,proposal)=>'<iframe sandbox="allow-same-origin" title="'+name+(proposal?' proposed':' current')+'" srcdoc="'+esc('<!doctype html><html><head><style>'+css+(proposal?fallback:'')+'</style><style>body{margin:16px;width:auto;background:white}html,body{height:auto}#MainPanel{width:auto}input{max-width:100%}</style></head><body class="'+(proposal?'us-native-form-preview':'')+'">'+read('prototypes/native-forms/'+name+'.html')+'</body></html>')+'"></iframe>';
const html='<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Native form integration</title><style>body{font:15px/1.5 system-ui;background:#f4f5f6;color:#24343b;margin:24px}main{max-width:1500px;margin:auto}.pair{display:grid;grid-template-columns:1fr 1fr;gap:20px}iframe{width:100%;height:760px;border:1px solid #ccd0d4;background:white;box-sizing:border-box}pre{overflow:auto;background:white;padding:16px}button{padding:8px 14px}@media(max-width:850px){.pair{grid-template-columns:1fr}}</style><main><h1>Native form integration — implemented</h1><p>Captured structures with field values cleared, hidden state removed and native handlers disabled. Both views now use the approved shared theme. The second also includes the offline file-chooser demonstration. No submission or upload occurs. Telerik scripts and resource skins are not included: widget operation and exact native skin appearance require live verification.</p><p>Native panel editors now use a 150px label column and 280px control column when their available column width is at least 460px. Narrower columns stack labels above full-width controls. Text fields, selects and the date-picker wrapper share a width; checkboxes and radios retain their intrinsic size. Address, Theme Upload and XML Import have their own scoped layouts. Hidden and disabled states remain native. Ordinary fields receive theme borders, focus and state colours. Telerik upload and combo controls receive only conservative border/surface styling; their file-input positioning and behaviour are untouched. The upload demos use one visible browser file chooser in place of the uninitialised Telerik duplicates; its Select/Remove skin controls are hidden only in this offline study. Approved form styling is included in zUnionSuite.css. The chooser fallback remains offline-only; live Telerik keeps Select/Remove.</p>'+items.map(n=>'<section><h2>'+n.replaceAll('-',' ')+'</h2><div class="pair"><div><h3>Current theme</h3>'+frame(n,false)+'</div><div><h3>Approved integration / upload demo</h3>'+frame(n,true)+'</div></div></section>').join('')+'<h2>Shared CSS and integration</h2><p>No author class is required for native fields. Upload zUnionSuite.css and zUnionSuite.js. Native upload validation remains authoritative; test selecting, removing and dropping a file in live iMIS. IQA rules remain separate.</p><button data-copy="css">Copy shared CSS</button><pre id="css">'+esc(trial)+'</pre><p id="status" role="status"></p><a href="index.html">All references</a></main><script>document.querySelectorAll("[data-copy]").forEach(b=>b.onclick=async()=>{try{await navigator.clipboard.writeText(b.dataset.copy==="class"?"us-native-form-preview":document.getElementById("css").textContent);document.getElementById("status").textContent="Copied"}catch{document.getElementById("status").textContent="Select the CSS and copy manually."}})</script></html>';
fs.writeFileSync('references/Native-Form-Integration.html',html.replace('</html>','<script>'+read('prototypes/Native-Form-Integration.js')+'</script></html>'));console.log('Built native form comparison');
