const fs=require('fs'),p='tools/build-theme-usage.cjs';let s=fs.readFileSync(p,'utf8');const a=s.indexOf('function staticBanner('),b=s.indexOf('const badgeParts',a);let part=s.slice(a,b);const x=part.indexOf('<style>'),y=part.indexOf('body{margin',x);part=part.slice(0,x)+'<style>'+String.fromCharCode(36)+'{theme}\\n'+String.fromCharCode(36)+'{clientRoots}\\n'+part.slice(y);fs.writeFileSync(p,s.slice(0,a)+part+s.slice(b));
