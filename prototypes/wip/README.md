# Work in progress

Create a folder per current idea, with a short README recording the problem,
status, maintained sources, preview command and decisions still open.

Keep useful WIP sources tracked. Put disposable screenshots, raw captures and
temporary probes under the repository-root ignored `archive/` or scratch paths.
When approved, move the specification to `../approved/`; implementation and guide
promotion happen before its supporting research is retired.
