# Deferred banner tab integration

Status: deferred design work; production zone switching is not implemented.
The [plan](Banner-Tabs-Plan.md) records the proposed adapter and open integration
work. The implemented visual banner template and its usage instructions live in
`THeme/UnionSuite/guides/usage/`. See the root `TODO.md` for the agreed scope.

This is a tracked WIP specification, not a guide build dependency. Update its
status and move it to `prototypes/approved/` when implementation requirements
are accepted; archive only after useful decisions have been retained.
